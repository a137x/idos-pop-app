{{- $ns := .Release.Namespace -}}
{{- $metrics := .Values.metrics -}}

apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: {{ include "idos-pop-dapp.fullname" . }}
  labels:
    {{- include "idos-pop-dapp.labels" . | nindent 4 }}
spec:
  groups:
  - name: idos-popDapp
    rules:
    - alert: DappHpaMaxedOut
      expr: >
        kube_horizontalpodautoscaler_status_current_replicas{namespace="{{ $ns }}",horizontalpodautoscaler="dapp"} == kube_horizontalpodautoscaler_spec_max_replicas{namespace="{{ $ns }}",horizontalpodautoscaler="dapp"}
      for: 15m
      labels:
        severity: critical
        tier: application
        application: idos-pop
        component: dapp
        namespace: {{ $ns }}
        cluster: {{ $metrics.alert_labels.cluster }}
        env: {{ $metrics.alert_labels.env }}
      annotations:
        summary: "idos-pop dapp HPA is running at max replicas"
        description: "idos-pop dapps has been running at max replicas for longer than 15 minutes"
        dashboard: {{ $metrics.dashboard_url }}
        runbook_url: "https://runbooks.extratools.works"
